# 生成物：内嵌你的真实项目名与用量数据，绝不要提交
dashboard.html
*.json
!package.json

# VPS 连接配置：含自己的服务器地址。已被上面的 *.json 覆盖，这里显式重申一遍
vps.local.json

# Coding Plan 查询凭据：仅供本机后端读取，禁止提交
coding-plans.local.json

# new-api 面板访问令牌：仅供本机后端读取，禁止提交
newapi.local.json

__pycache__/
*.py[cod]

# 浏览器验收（Playwright MCP）的临时快照/截图
.playwright-mcp/

.DS_Store
.venv/

# 浏览器验收的临时截图
*.png
